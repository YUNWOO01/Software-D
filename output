#include <stdio.h>     
#include "header.h"    

void output(int req, int p1, int p2)
{
    // 要求（connect/disconnect）を文字列に変換
    const char* req_str = (req == connect) ? "CONNECT" : "DISCONNECT";

    // 仕様書 [cite: 24] に基づき、処理内容を表示
    printf(">> OUTPUT: %s ( %d, %d )\n", req_str, p1, p2);

    
}
